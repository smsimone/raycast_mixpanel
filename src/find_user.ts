/**
 * Searches for a user given it's name and/or surname
 */
export default function searchUser(data: { name: string | undefined, surname: string | undefined, email: string | undefined }) {
	return undefined;
}